{
  "name": "djs-next-dashboard",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "pnpm --parallel --stream -r run dev",
    "build": "pnpm --stream -r run build",
    "format": "prettier --write . --ignore-path .gitignore"
  },
  "dependencies": {
    "@types/node": "^18.16.0",
    "prettier": "^2.8.8",
    "prettier-plugin-tailwindcss": "^0.3.0",
    "typescript": "^5.1.3"
  }
}
